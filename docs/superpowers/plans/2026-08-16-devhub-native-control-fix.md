# DevHub Native Control Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make DevHub's Language dropdown consistently dark and remove the inherited white frame around its Ready status bar.

**Architecture:** Keep the native select and the existing semantic footer. Add narrowly scoped, `!important` DevHub declarations only where necessary to beat the shared shell combobox rule, and explicitly reset the 7.css footer margin, border, shadow, and pseudo-element chrome.

**Tech Stack:** CSS, vanilla JavaScript DOM output, Node.js built-in test runner.

## Global Constraints

- Do not change shared `desktop.css` or `vendor/7.css` behavior.
- Keep the Language control native and keyboard accessible.
- Preserve the status bar's existing `#30363d` top separator.
- Bump only the DevHub stylesheet cache key to `v=20260816-4`.
- Keep all changes uncommitted unless the user explicitly asks for a commit.

---

### Task 1: Scope Dark Native Controls and Footer Reset to DevHub

**Files:**
- Modify: `tests/devhub-app.test.js`
- Modify: `devhub.css`
- Modify: `index.html`
- Modify: `docs/superpowers/specs/2026-08-16-devhub-design.md`

**Interfaces:**
- Consumes: `.devhub-repository-toolbar select`, its native `option` elements, and `footer.devhub-statusbar` generated by `DevHubApp`.
- Produces: a dark native select popup and a frameless status bar with only its top separator.

- [x] **Step 1: Add the failing CSS cascade regression test**

Read the scoped rules from `devhub.css` and assert that the exact DevHub select selector outranks the shared shell's base, hover, active, and focus selectors while declaring dark color-scheme, dark option colors, and `!important` visual overrides; assert the footer resets margin, side borders, shadow, and its inherited `::before`; assert `index.html` loads `devhub.css?v=20260816-4`.

- [x] **Step 2: Run the focused test and confirm RED**

Run: `node --test --test-name-pattern="dark native control" tests/devhub-app.test.js`

Expected: FAIL because the current DevHub rule loses to shared `!important` combobox declarations and the status footer retains 7.css chrome.

- [x] **Step 3: Add the minimal scoped CSS overrides and cache bump**

In `devhub.css`, set `color-scheme: dark` on `.devhub-app`, use the exact selector `#windows-7-root .devhub-window .devhub-repository-toolbar select[data-devhub-language]:not([multiple]):not([size])` for the dark arrow/background, and add dark `option` colors. Reset `margin`, `border`, and `box-shadow` on `footer.devhub-statusbar` and suppress its `::before`. In `index.html`, load `devhub.css?v=20260816-4`.

- [x] **Step 4: Run focused and full verification**

Run:

```powershell
node --test tests/devhub-app.test.js
node --check devhub.js
npm.cmd test
git diff --check
```

Expected: all commands exit successfully with zero failing tests.
